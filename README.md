# Asteroid-Thread-Detection-Analysis
This was my project of CSE437 (Data Science) project 
where we used different ML models from sklearn libraries to predict if an asteroid would be a threat or not. 
